window.libraryName = window.libraryName || "new lib 2";

// In the above code, we are checking if a global variable with the name called libraryName exists in the window object. if it exists then it value will remain same. If it doesn't exist then assign the value we want to assign it.